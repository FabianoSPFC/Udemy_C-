using System;
using System.Globalization;
using System.Collections.Generic;
using System.Text;

namespace Secao3
{
	/*Fazer um programa que leia o número de um funcionário, seu número de horas trabalhadas, o valor que recebe por
	hora e calcula o salário desse funcionário. A seguir, mostre o número e o salário do funcionário, com duas casas
	decimais.

	class Exercicio4
	{
		static void Main(string[] args)
		{
			Console.Write("Digite o número do funcionário: ");
			int Numero = int.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);
			Console.Write("Digite a quantidade de horas que o funcionário trabalha: ");
			double QtdHoras = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);
			Console.Write("Digite o salário do funcionário: ");
			double Salario = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);

			double resultado = funcionario(QtdHoras, Salario);

			Console.WriteLine($"O funcionário {Numero} possui salário de {resultado:F2}");
		}

		static double funcionario(double HorasTrabalhadas, double Salario)
		{
			return Salario * HorasTrabalhadas;
		}
	}*/
}
